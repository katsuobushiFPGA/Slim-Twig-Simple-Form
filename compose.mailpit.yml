services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: slim-mailpit
    ports:
      - "8025:8025"  # Web UI
      - "1025:1025"  # SMTP
    networks:
      - slim-network
    restart: unless-stopped
